#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>
int main() {
    int n,a[100];
    int max=a[0];
    int k,i,j=0; 
    scanf("%d %d", &n, &k);
    for(i=1;i<=n;i++)
    {   scanf("%d",&a[i]);
                   }
    for(i=1;i<=n;i++)
    {     if(a[i]>max)
        max=a[i];
         }
    if(max>k)
        printf("%d",(max-k));
    else
        printf("0");
    
    
    return 0;
}
